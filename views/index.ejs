<!DOCTYPE html>
<html>
<head>
    <title>Personalized Joke</title>
    <!-- Use styles from styles.css so it's pretty -->
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Get a Personalized Joke!</h1>
    <!-- This form lets people type their name and ask for a joke -->
    <form action="/joke" method="POST">
        <input type="text" name="name" placeholder="Enter your name" required>
        <button type="submit">Get Joke</button>
    </form>
    <!-- If there is a joke, show it in green! -->
    <% if (joke) { %>
        <p class="joke"><%= joke %></p>
    <% } %>
    <!-- If there is an error, show it in red! -->
    <% if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>
</body>
</html>
